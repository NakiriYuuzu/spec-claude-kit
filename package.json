{
  "name": "spec-claude-kit",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run server:dev && bun run client:dev",
    "build": "bun run client:build",
    "preview": "bun run build && bun run server:preview",
    "start": "bun run build && bun run server:start",
    "server:dev": "cd server && bun run dev",
    "server:start": "cd server && bun run start",
    "server:preview": "cd server && bun run preview",
    "client:dev": "cd client && bun run dev",
    "client:build": "cd client && bun run build",
    "client:preview": "cd client && bun run preview",
    "install:all": "bun install && cd client && bun install && cd ../server && bun install"
  },
  "workspaces": [
    "client",
    "server"
  ]
}